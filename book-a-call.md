# "Book a call" setup

The site links to `https://cal.com/haseebasif` in the profile
sidebar on index.html and the calendar icon in the footer of all four pages.

## Before this goes live, double check

1. **Availability** is set under your Cal.com dashboard, in your local
   timezone, to hours you actually want to take calls.
2. At least one **event type** exists (e.g. "30 Min Chat"). Without one, the
   link lands on an empty page instead of a bookable slot.
3. Open `https://cal.com/haseebasif` yourself once and click through
   a test booking to confirm it works end to end.

If you ever change your Cal.com username, find and replace
`haseebasif` across `index.html`, `projects.html`, `research.html`, and
`cv.html` (6 occurrences total), or tell me the new one and I'll swap it.
